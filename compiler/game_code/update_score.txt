test: nop
	
	addi $t0, $r0, 45
	addi $t1, $r0, 2345
	sw  $t1, 0($t0)



update_score: nop

	# $a0: value to add

	addi $s0, $r0, 45				# get address 45
	lw $s1, 0($s0)					# load score from address 45
	add $s1, $s1, $a0				# score += arg0
	sw $s1, 0($s0)					# store new score

	jr $ra